<section class="section">
  <h2 class="section-title">{{title}}</h2>
  <table class="totals-table">
    <tbody>
      {{#each items}}
        <tr>
          <th>{{label}}</th>
          <td>{{value}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</section>
